---
description: Start the high-assurance full workflow with automatic worker dispatch
agent: plan
subagent: false
---

Call `workflow_start` with `profile: "full"`, the requested goal, and `dispatch: true`. Full requires the product-design and implementation-plan approvals; it uses requirements, design, architecture, traceability, TDD planning, independent task review, integration review, and completion evidence.
